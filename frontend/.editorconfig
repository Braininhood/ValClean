# Enforce LF line endings to avoid webpack "unterminated comment" on Windows
# (CRLF + multiline comments in generated chunks can break parsing)
root = true

[*]
end_of_line = lf
indent_style = space
charset = utf-8

[*.{js,ts,tsx,jsx,css,json,md}]
indent_size = 2
